// Package web embeds the built SPA (see embed.go). This file exists only
// to keep the package documented and importable in tests without the
// Go toolchain treating the directory as mixed content (the dist files
// are data, not source).
package web
